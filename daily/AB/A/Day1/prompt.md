# Day1 提示词

本文件归档 Day1 A 的工程规范、提示词方法和可复用设计资源相关提问。软件设计与状态机部分按当前分工归入 Day1 B。

## ChatGPT 会话一 · 提问清单

> 来源：ChatGPT 分享会话一的按时间顺序提问清单（全表 31 条，编号 C01–C31 与全表一致，此处只列归属本 Day 的条目）。

- **C31 | 原话（摘要）：**「列出你问过的所有问题」 *（提示词归档本身的操作，对应本文件的整理口径，不构成开发任务。）*

## ChatGPT 会话一 · 需求与约束

> 来源：ChatGPT 分享会话一中除提问以外的需求、约束与执行要求汇总，按原文分组保留，此处只列归属本 Day 的条目。敏感凭据已脱敏。

**CI、PR 与部署**

- 使用 `feature/*` 分支开发。
- 必须通过 PR 合入 `main`。
- 采用 squash merge，不直接推送 `main`。
- PR 必须包含 Human Design、Agent Self-Claimed、Validation 三段。

## ChatGPT 会话二 · 提问清单

> 来源：ChatGPT 分享会话二的按时间顺序清单（问题 E01–E15、非问句需求 R01–R11，编号与全表一致，此处只列归属本 Day 的条目）。

- **E06 | 原话（摘要）：**「目前所有 PR 评论是否有道理、是否值得修改」 *（对应 PR 评审粒度与“检查通过后 squash”的协作规则。）*
- **E12 | 原话（摘要）：**「这不是 `feat`，应该是 `docs` 吗」 *（Conventional Commits 的类型判定，写入提交规范。）*
- **R03 | 需求（交叉）：**创建 GitHub 仓库和 OpenAPI CI *（仓库与分支基线在本 Day；OpenAPI CI 在 Day2 A 落地。）*
- **R04 | 需求：**取消 `dev` 分支 *（确立 `main` 为唯一集成与部署基线，只保留 `feature/*`。）*
- **R05 | 需求：**设置仓库为 public
- **R07 | 需求：**安装并配置 skills *（对应 `skills-lock.json` 的版本锁定。）*
- **R09 | 需求：**不要修改 skill 相关内容 *（锁定后不得由代理自行改动，保证执行可复现。）*
- **R11 | 需求：**把最终说明直接写进 `AGENTS.md` *（工程与代理规则集中在单一入口。）*

## ChatGPT 会话二 · 需求与约束

> 来源：ChatGPT 分享会话二的按时间顺序清单，内容为非问句的需求、约束与修正（N01–N29），末尾另记需求覆盖（O01–O04）。本清单是会话二 R01–R11 的展开版本，编号与全表一致，此处只列归属本 Day 的条目。

- **N03 | 需求：**站内聊天、收藏、交易状态流转都要实现 *（确定 MVP 范围内能力，写入 `README`；状态机侧在 Day1 B 交叉。）*
- **N05 | 需求：**先在 GitHub 创建同名 public 仓库
- **N10 | 约束：**`main` 和开发分支只能通过 PR 合入，不需要同行评审 *（两人协作下用必需检查代替人工评审。）*
- **N11 | 需求：**执行 `npx --yes skills add KDZZZZZZ/codex-agents-md-generator --skill codex-agents-md-generator --agent codex --global --yes`
- **N12 | 需求：**通过该 skill 生成或更新 `AGENTS.md`
- **N13 | 需求：**加强 Git 使用规范，采用 squash 合并
- **N14 | 需求：**后续明确取消 `dev` 分支 *（见 O02。）*
- **N15 | 约束：**所有仓库和服务使用 public 设置
- **N17 | 需求：**安装截图中列出的 skills，并加入 Git 忽略规则 *（对应 `skills-lock.json` 与 `.gitignore`，本地产物不进仓库。）*
- **N18 | 约束：**在 `AGENTS.md` 中规定，当需求无法直接转化为代码，或涉及性能、安全但未指定实现方式时，先查找成熟参考 *（成熟工程参考的具体检索在 Day1 B 交叉。）*
- **N22 | 需求：**查看当前所有 PR 评论，判断是否合理、是否值得修改
- **N23 | 约束：**skill 相关内容不要修改 *（锁定后不得由代理自行改动。）*
- **N24 | 约束：**除 skill 相关内容外，其他合理修改可以直接完成
- **N27 | 需求（交叉）：**前端远程联调的那句话直接写入 `AGENTS.md` *（方案本身在 Day4 B。）*
- **N28 | 修正：**文档类修改使用 `docs` 类型，而不是 `feat` *（见 O04。）*

需求覆盖：

- **O01 | 覆盖（交叉）：**「砍掉聊天、收藏、交易状态」→「聊天、收藏、状态流转都要做」 *（范围回滚，`README` 的范围内能力据此定稿。）*
- **O02 | 覆盖：**「创建 `dev` 分支」→「取消 `dev` 分支」 *（最终以 `main` 为唯一集成与部署基线。）*
- **O04 | 覆盖：**「使用 `feature` 类型」→「文档修改使用 `docs` 类型」 *（Conventional Commits 类型口径定稿。）*

## DOCX 整理版提示词

> 我正在做一个校园二手交易平台，前后端由两个人分工开发。请先建立可执行的工程协作基线：整理 `AGENTS.md`、`skills-lock.json` 和 README，采用结构化提示词拆解需求，写清每一步的文件、验收方式和待确认事项；同时盘点可复用的设计与 skills 资源，避免引入不必要的付费依赖。软件设计与状态机文档由 Day1 B 负责。
