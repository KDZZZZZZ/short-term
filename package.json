{
  "name": "short-term-api-contract",
  "version": "1.0.0",
  "private": true,
  "description": "OpenAPI contract and deployment tooling for the campus second-hand marketplace MVP",
  "scripts": {
    "openapi:config": "redocly check-config",
    "openapi:lint": "redocly lint campus-market@v1",
    "openapi:bundle": "redocly bundle campus-market@v1 --output openapi/openapi.bundle.json --ext json --remove-unused-components --component-renaming-conflicts-severity error",
    "openapi:check": "npm run openapi:config && npm run openapi:lint && npm run openapi:bundle && git diff --exit-code -- openapi/openapi.bundle.json",
    "proto:build": "buf build",
    "proto:format": "node scripts/proto-format.mjs --write",
    "proto:format:check": "node scripts/proto-format.mjs",
    "proto:lint": "buf lint",
    "proto:generate": "buf generate",
    "proto:check": "npm run proto:build && npm run proto:format:check && npm run proto:lint && npm run proto:generate && git diff --exit-code -- gen/go/shortterm",
    "go:vet": "go vet ./gen/go/... ./services/account/... ./services/favorite/... ./services/gateway/... ./services/marketplace/... ./services/messaging/...",
    "go:test": "go test -shuffle=on ./gen/go/... ./services/account/... ./services/favorite/... ./services/gateway/... ./services/marketplace/... ./services/messaging/...",
    "go:test:race": "go test -race -shuffle=on ./gen/go/... ./services/account/... ./services/favorite/... ./services/gateway/... ./services/marketplace/... ./services/messaging/..."
  },
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10"
  },
  "devDependencies": {
    "@bufbuild/buf": "1.72.0",
    "@redocly/cli": "2.49.0"
  }
}
