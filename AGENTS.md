# 项目级代理规则

## 人类摘要

| 主题 | 项目规则 | 详情 |
| --- | --- | --- |
| 范围 | 校园二手交易平台 MVP；推荐算法和邮件通知不在范围内 | [范围与优先级](#范围与优先级) |
| 基线 | `main` 是唯一集成和部署基线；没有 `dev`/`develop` | [GitHub 工作流](#github-工作流) |
| Pull Request | `feature/* -> main`，只能 PR 合入，0 个审批，CI 通过后 squash | [Pull Request 与署名](#pull-request-与署名) |
| 设计 | OpenAPI 是公开接口唯一真源，状态机文档治理跨资源转换 | [设计治理](#设计治理) |
| Skills | 按任务类型选择最小充分 skill，读取完整说明后行动 | [Skills 路由](#skills-路由) |
| 参考 | 触发条件下必须先找成熟参考，再作设计或实现决定 | [成熟参考触发器](#成熟参考触发器) |
| 研究 | 当前不是研究项目，不创建空研究目录 | [研究版本树](#研究版本树) |
| 验收 | 契约、Git 治理和部署必须分别提供真实证据 | [充分验收](#充分验收) |

## 范围与优先级

本文件约束仓库根目录及全部子目录中的人类和代理工作。更近目录中的 `AGENTS.md` 只能在其明确声明的目录范围内覆盖根规则。

规则冲突按以下顺序处理，并明确报告冲突，不能为方便而静默选择：

1. 当前对话中用户的明确指令。
2. 当前路径有效的 `AGENTS.md` 链。
3. 已批准的治理文档。
4. 已确认的 issue、PR 或任务契约。
5. 现有兼容性和回归测试。
6. 当前实现。
7. 外部参考。

当前产品范围：

- 学号和密码注册登录，不接统一身份认证。
- 用户资料及微信/QQ 联系方式。
- 商品发布、最多三张图片、分类、浏览、标题搜索和上下架。
- 商品收藏。
- 商品上下文中的一对一文字聊天和已读状态。
- 购买意向、卖家接受、取消、双方确认和商品联动状态。
- 用户不是固定买家或卖家；身份由商品和交易关系决定。
- 不实现推荐算法和邮件通知。

## 用户期望模型

每项任务开始时维护并在交付时复核：目标、可观察结果、范围、排除项、API/数据/安全不变量、外部写入授权、偏好、验收证据、假设和未决问题。

- 用户明确提出或确认的决策标为 `Human Design`。
- 从代码、文档、惯例、skill 或外部参考推断的决策标为 `Agent Self-Claimed`。
- 后续人类修正覆盖此前代理推断；沉默不等于批准。
- 低风险、可逆、不会改变产品或治理含义的细节可以继续，但必须披露。
- 会改变公开行为、API、架构、依赖、数据、安全、迁移、部署暴露面或主要成本的歧义必须询问用户。
- 产品歧义不能通过复制参考仓库解决。
- 交付必须说明与期望模型的偏差和剩余风险。

当前明确授权：

- 为已要求的工作创建并推送 `feature/*`，创建/更新 PR，在必需检查通过后 squash 合入 `main`。
- `main` 更新后由已配置的 GitHub Actions 自动部署 OpenAPI 文档。
- 不需要同行审批。

未包含在持续授权中的操作：删除仓库或云资源、扩张公网暴露面、改变仓库可见性、修改分支保护、购买服务、读取或导出无关凭据。遇到这些操作必须获得新的明确指令。

## GitHub 工作流

仓库事实：

- 远端：`https://github.com/KDZZZZZZ/short-term.git`。
- 可见性：public。
- 唯一基线和默认 PR 目标：`main`。
- 不存在也不得创建 `dev`、`develop` 或长期集成分支。
- `main` 对管理员同样强制保护：只能 PR 合入、0 个批准、禁止强推和删除、要求线性历史。
- 仓库允许 squash merge，禁用 merge commit 和 rebase merge；合入后自动删除远端 feature 分支。
- OpenAPI 检查在首次成功运行后必须加入 `main` 的必需状态检查。

### 临时分支

统一使用：

```text
feature/<short-topic>
```

要求：

1. 从最新 `origin/main` 创建。
2. 一个任务、一个分支、一个 PR。
3. 名称使用小写英文和连字符。
4. 不混入无关清理、格式化或重构。
5. 保留用户未提交变更；共享目录冲突时使用独立 worktree。
6. 提 PR 前同步最新 `origin/main`；重写自己的 feature 分支只能使用 `--force-with-lease`。
7. 只能通过 GitHub squash 合入 `main`，禁止本地向 `main` 合并后推送。
8. 分支仅在确认 PR 已合入或用户明确放弃后删除。
9. 不允许用 cherry-pick 绕开 PR 和必需检查。

提交与 PR 标题遵循 `CONTRIBUTING.md` 中的 Conventional Commits 规则。禁止直接推送或改写 `main` 历史。

## Pull Request 与署名

PR 正文必须包含：

```markdown
## Human Design

- 仅列人类明确要求或确认的行为、API、命名、架构、依赖、兼容性、安全和验收决策。

## Agent Self-Claimed

- 列代理自行引入的文件、类型、命名、实现方法、抽象、依赖、测试组织、重构、文档、CI 和部署细节；无则写 `None`。

## Validation

- 列实际执行的命令、退出码、结果以及未运行的关键门禁。
```

不得用 `misc`、`chore`、`also` 或“顺便”等标签掩盖代理主动扩大范围。PR 标题是 squash 后的 `main` 提交标题，必须准确且符合 Conventional Commits。

## 设计治理

| 范围 | 治理文档 | 批准状态 | 何时必须遵循 |
| --- | --- | --- | --- |
| 架构 | 当前无独立架构文档 | 未建立 | 模块边界、技术栈或部署拓扑变化前需由人类确认是否创建 |
| 公开 API | `openapi/openapi.yaml` 及其引用文件 | 已批准基线 | 路径、字段、错误码、鉴权、上传或响应变化 |
| 数据与交易 | `docs/state-machines.md` | 已批准基线 | 商品、收藏、消息、交易、并发和持久化变化 |
| 安全 | OpenAPI 权限描述、本文及 `CONTRIBUTING.md` | 部分建立 | 认证、授权、联系方式、上传、密钥或公网边界变化；重大设计需新文档 |
| UI | 无 | 未建立 | 用户要求 UI 时先确认设计依据 |
| 测试 | `package.json`、`.github/workflows/openapi-ci.yml` | 已批准基线 | 契约或构建门禁变化 |
| 研究 | 无 | 不适用 | 引入实验、基准或模型研究时建立实际研究目录 |
| 发布 | `.github/workflows/deploy.yml`、`deploy/short-term-openapi.service` | 已批准基线 | 部署触发器、主机、端口、容器或回滚策略变化 |

公开 API 规则：

- `openapi/openapi.yaml` 是源契约；`openapi/openapi.bundle.json` 是生成产物，禁止手工编辑。
- 先改契约和状态机，再改实现、客户端和测试。
- 商品或交易状态不能通过通用 `PATCH` 任意赋值，只能使用指定动作接口。
- 公开 ID 是不透明字符串；客户端不得依赖数据库类型或 ID 结构。
- 金额使用十进制字符串，禁止通过 JSON 二进制浮点传输。
- 商品详情不得泄露卖家学号；密码只保存不可逆哈希，永不进入响应或日志。
- 后端加入后必须增加实现对 OpenAPI 的契约测试和状态机并发测试，不能以代码注解反向覆盖契约。

## 前端远程后端开发

如本地后端环境不方便启动，可在服务器后端可用时直接连接该服务进行前端开发与联调。

## Skills 路由

| 任务类型 | 必需 skill | 触发条件 | 说明 |
| --- | --- | --- | --- |
| 创建或刷新根代理规则 | `codex-agents-md-generator` | 用户要求生成/更新 `AGENTS.md` | 只用于指导文件，不用于普通功能实现 |
| 实现、重构和验证代码 | `Code` | 用户要求写或修改代码 | 按计划、实现、验证、交付执行 |
| 分支、提交、PR 和历史操作 | `git-essentials` | 任何 Git 写操作或历史判断 | 项目规则优先于 skill 中通用示例 |
| 无法直接固化为代码、契约或测试的需求 | `software-designer` + `source-driven-development` | 需要代理决定产品语义、架构或非功能实现 | 先调查成熟参考，再列明事实、约束、选项和代理主张 |
| Go 任务 | `golang-how-to` + 最小相关 `golang-*` skill | 编写、修改、评审或排障 Go | 先用路由 skill，再按具体领域选择专长 skill |
| Go 性能决策 | `golang-benchmark` + `golang-performance` | 涉及性能且目标、方法未指定，或方案由代理选择 | 先找成熟参考并建立基线，只报告实测结论 |
| Go 安全决策 | `golang-security` + `security-auditor` | 涉及安全且实现未指定，或方案由代理选择 | 先找成熟参考和威胁模型，再验证边界与失败路径 |
| 缺少回归保护的重构 | `refactor-safety-net` | 重构前覆盖不足 | 先锁定行为，再做小步变更 |
| 仓库级过度设计审计 | `ponytail-audit` | 用户明确要求全局审计 | 只读审计，不能代替具体实现或评审 |
| 外部框架、库、API 或 Action | `source-driven-development` | 正确性依赖具体版本 | 读取依赖版本，使用官方文档和固定版本 |
| 认证、授权、上传、输入验证和秘密 | `security-auditor` | 涉及安全边界或安全评审 | 检查所有权、最小权限、泄漏和攻击面 |
| 安装第三方 skill | `skill-vetter` + `skill-installer` | 用户明确要求安装 | 先完整审查来源和文件，再安装；不得输出凭据 |

代理必须：

1. 按任务类型检查 skill，选择最小充分集合。
2. 在行动前告诉用户使用哪些 skill 以及原因。
3. 完整读取每个选中 `SKILL.md` 及其要求的直接参考。
4. 不从名称猜测 skill 行为。
5. 用户点名的 skill 必须使用；不可用时说明并采用最接近的安全方案。
6. 用户明确要求优先于 skill 的冲突性通用建议，并在交付和 PR 署名中披露偏离。
7. 安全、认证、加密、数据库事务、并发、性能、框架、协议、UI、CI/CD、供应链或研究任务若没有已安装专长 skill，先搜索 skill，再使用官方规范和上游实现。

## 成熟参考触发器

出现以下任一情况时，必须在设计或编码前先调查成熟参考：

1. 人类需求无法直接转换为可执行代码、公开契约、自动化测试或明确实现约束。
2. 人类需求涉及性能或安全，但没有指定目标、威胁模型、实现方法或验收方式。
3. 存在会影响公开行为、架构、依赖、数据、安全、部署暴露面或主要成本的多个方案，需要代理自行选择。

执行要求：

1. 先核对当前对话、仓库治理文档、公开契约、测试和现有实现，区分已知事实、强约束、待选方案和未知项。
2. 按 [参考来源](#参考来源) 的顺序查找证据，优先官方规范、官方文档、上游实现与测试、维护活跃且经过生产验证的项目；博客和论坛只能提供线索。
3. 记录所用参考的版本、提交或具体文件，说明可借鉴行为、适用前提、取舍以及与本项目的差异，禁止无上下文复制。
4. 由参考推导出的选择必须标为 `Agent Self-Claimed`。参考不能替代人类产品意图；若选择会产生重大产品或治理含义，调查后仍须向用户确认。

性能决策还必须先定义可观察指标、目标和同环境基线，再用可复现基准比较候选方案；没有测量证据不得声称性能改善。安全决策还必须先识别资产、信任边界、攻击者可控输入和失败影响，形成与当前范围相称的威胁模型，再验证认证、授权、输入处理、秘密、日志及失败路径。

## 参考来源

调查顺序：

1. 当前对话和用户期望模型。
2. 本仓库治理文档。
3. 测试和公开接口。
4. 当前实现和 Git 历史。
5. 已配置参考仓库。
6. 官方规范、文档和上游实现。
7. 其他成熟生产项目。

| 子系统 | 参考仓库 | 相关位置 | 用途 |
| --- | --- | --- | --- |
| OpenAPI | `https://github.com/OAI/OpenAPI-Specification` | OAS 3.1 规范 | 公开 HTTP 契约语义 |
| 契约校验 | `https://github.com/Redocly/redocly-cli` | `lint`、`bundle` 文档与当前锁定版本 | lint 和生成 bundle |
| API 文档容器 | `https://github.com/swagger-api/swagger-ui` | Docker 安装及配置文档，锁定 Swagger UI 版本 | 部署 Swagger UI |
| 国内镜像加速 | `https://github.com/DaoCloud/public-image-mirror` | `m.daocloud.io` 前缀和摘要保持规则 | ECS 拉取上游镜像；必须固定 `sha256` 摘要 |

使用外部参考时必须记录具体版本、文件或提交，并比较行为与取舍；本地人类要求和已批准设计优先。引用产生的新设计属于 `Agent Self-Claimed`，直至人类确认。博客和论坛只能作为线索，不能作为唯一依据。

当框架或协议是新的、来源冲突、版本或安全建议可能变化、存在高风险一致性/恢复语义，或现有参考只能证明“可以”而不能证明“适合”时，必须补充当前官方证据。

## 研究版本树

当前仓库不包含实验、模型、算法研究或基准，不创建空 `research/` 脚手架。

如果将来出现研究任务，必须建立可恢复的逻辑版本树，记录父节点、假设、单一关键变化、环境与依赖版本、命令、原始结果、指标、结论、限制和 `candidate/accepted/rejected/inconclusive/superseded` 状态。关键代码和结果必须由 commit、tag、短期分支、patch 或不可变制品恢复；失败结果不能被覆盖删除。

## 充分验收

不变量：通过验收应让用户目标已经实现具有高置信度，而不只是文件能解析或单个示例通过。

| 用户目标 | 前置条件 | 输入/动作 | 可观察结果 | 证明 | 失败含义 |
| --- | --- | --- | --- | --- | --- |
| OpenAPI 是有效唯一契约 | 锁定 Node/npm 依赖 | `npm ci && npm run openapi:check` | lint、bundle 和 Git diff 均成功 | 命令退出码 0，bundle 无漂移 | 契约无效或生成产物漂移 |
| 所有变更经 PR squash 合入 | GitHub 分支保护可读 | 查询仓库设置和 `main` protection | 0 审批、PR 必需、线性历史、禁强推/删除、仅 squash | GitHub API 实际响应 | Git 治理未强制执行 |
| API 状态转换一致 | 后端和数据库实现存在 | 并发创建/接受/取消/确认交易 | 结果符合 OpenAPI 和状态机且最多一个已接受交易 | 独立契约测试、事务集成测试和并发测试 | 可能超卖或商品/交易状态分裂 |
| `main` 自动部署文档 | Actions secrets 和 ECS 就绪 | 合入一次已验证变更 | deploy workflow 成功，远端容器健康且返回 Swagger UI | GitHub run 日志、远端容器状态和真实 HTTP 请求 | 自动部署或运行服务不可用 |
| 敏感信息不泄漏 | 认证与上传实现存在 | 访问详情、错误路径和日志 | 不返回学号、密码、Token、密钥或无权资源 | 安全测试和日志检查 | 隐私或凭据泄漏 |

通用验证要求：

- 先运行受变更直接影响的最小测试，再运行 lint、类型检查、构建、集成和回归检查。
- 缺陷修复必须有能对旧行为失败的回归证据。
- 验证必须使用真实入口和关键组件；mock 不能替代必要的数据库、并发、上传或部署集成。
- 验收 oracle 必须来自人类需求、已批准设计、官方规范或既有兼容性契约，不能仅来自当前实现。
- 报告精确命令、退出码、关键结果和未运行门禁。

结果定义：

- `PASS`：每个关键目标都有充分、独立且可复现的证据。
- `FAIL`：指出失败要求、证据及其对用户目标的影响。
- `INCONCLUSIVE`：指出缺失证据、已尝试替代方案和重新验证条件；关键门禁未运行时禁止声明 `PASS`。

交付前回答：是否存在检查都通过但用户目的仍失败的现实路径；是否使用真实入口；oracle 是否独立；证据能否捕获目标缺陷；边界、无效输入、权限、状态、持久化、并发、恢复和相邻回归是否按风险覆盖；环境是否具有代表性。
